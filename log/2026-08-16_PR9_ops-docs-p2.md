# PR #9 — fix: dependency/advisory scanning + vertical-slice FE self-check

Date: 2026-08-16
PR: https://github.com/JustMath-Web/website/pull/9
Branch: `fix/ops-docs-p2`
Merge commit: `08fea72`

## What Shipped

- **VS-09**: enabled GitHub's native Dependabot vulnerability alerts and automated security-fix PRs
  on the repo (both were off by default — checked via `gh api`, not assumed); added
  `.github/dependabot.yml` for weekly version-update PRs (`web/`, `studio/`, GitHub Actions); added
  `pnpm audit --audit-level=critical` to both CI jobs.
- **VS-12**: the vertical-slice FE self-check guideline Section 9 requires, written for the first
  time (retroactively, on commit `331f8ac`). Re-ran the checkable gates fresh rather than
  transcribing Bob's own independent audit; attributed live-browser claims explicitly where citing
  his evidence instead of re-deriving it a third time.
- Rebased onto PR #7 and #8 after they merged, in that order — `docs/DECISIONS.md`/`HANDOFF.md`
  tail-append conflicts resolved, renumbering the ops/docs section to §23. Left the VS-12 self-check
  table as an accurate record of the commit it actually assessed rather than silently rewriting it to
  claim FE-32 now passes; added an explicit rebase note instead.

## Root Cause

N/A — new capability (scanning, documentation), not a bug fix.

## Review Notes (Bob)

Fixes VS-09 and VS-12 from Bob's 2026-08-15 review (`review/bob/CODE-REVIEW.md`). Not independently
re-reviewed by Bob before merge — Charlie reviewed and merged directly.

## Deferred

- Two pre-existing non-critical dependency advisories (`path-to-regexp` high, `undici`
  moderate/high ×11 — both build-tool-only, not runtime code) recorded as accepted exceptions in
  `docs/DECISIONS.md` §23 with owner/rationale/review-date, not fixed in this PR.
- VS-12's FE-32 finding (FAQ no-JS reachability) was fixed by PR #7 but not re-verified fresh as part
  of this PR's rebase — flagged as a stale row in the self-check table, not silently corrected.
- Remaining P2/P3 items: VS-10 (fonts) not started yet; VS-15/VS-17 stay queued (blog routes / more
  templates don't exist yet).
