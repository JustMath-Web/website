# Dependencies
node_modules/
.pnpm-store/
/.pnp
.pnp.js

# Build output
web/dist/
studio/dist/
web/.astro/
web/.vercel

# Sanity Studio runtime/build artifacts
studio/.sanity/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
/logs

# Session continuity / bookkeeping — local-only per Charlie's 2026-08-17 decision (docs/DECISIONS.md
# §25). Real files, kept on disk across sessions for Charlie's and the AI's own tracking, but never
# committed or pushed — not `/logs` above, which is unrelated (generic tool log output).
/HANDOFF.md
/log/

# Environment variables (never commit real secrets — .env.example files are tracked)
.env
.env.local
.env.production
.env.*.local
*.local

# Coverage
coverage/

# Playwright
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/

# OS files
.DS_Store
*.pem

# Editor
.idea/

# Playwright MCP session snapshots (transient, not a project artifact)
.playwright-mcp/

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
