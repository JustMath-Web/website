/** The official WhatsApp glyph (Simple Icons), inherits currentColor. */
export interface WhatsAppGlyphProps {
  /** square size in px, default 20 */
  size?: number;
  style?: React.CSSProperties;
}
export declare function WhatsAppGlyph(props: WhatsAppGlyphProps): JSX.Element;
