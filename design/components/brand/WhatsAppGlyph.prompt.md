The WhatsApp mark — only ever inside a control that actually opens WhatsApp.

```jsx
<WhatsAppGlyph size={18} />
```

Use it via `WhatsAppButton` in almost every case. Never recolour the surrounding control green unless this glyph is present, and never use this glyph decoratively.
