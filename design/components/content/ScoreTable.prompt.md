Marks, attendance, homework returned — the factual half of a report.

```jsx
<ScoreTable columns={["Assessment", "Score", "Out of"]} rows={[["Chapter 3 test", 34, 40]]} caption="March 2026" />
```

Hairline rules, no zebra striping, no card wrapper. First column sans, figures mono and tabular so columns line up.
