Short, closed lists: form level, syllabus, lesson slot.

```jsx
<Select placeholder="Choose a syllabus" options={["KSSM", "IGCSE", "SPM revision"]} />
```

Matches Input metrics exactly. Use a `RadioGroup` instead when there are three or fewer options and the choice matters to the parent.
