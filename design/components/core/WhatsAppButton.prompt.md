The booking action. Green here is a promise: this opens WhatsApp.

```jsx
<WhatsAppButton size="lg" phone="60123456789" message="Hi, my daughter is in Form 3." note="Usually replies the same day." />
```

Rules: no other element in a Just Math layout may be green. Always prefill `message` with something a parent would actually send. Pair with `note` for reassurance rather than urgency copy. One per screen section — repeated green loses its meaning.
